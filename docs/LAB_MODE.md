# Режим РАЗБОР — полный разбор

Главный исследовательский экран: один участок целиком, решётка тактов×каналов
с курсором, и шесть панелей, каждая отвечает на свой вопрос про клетку под
курсором. Открывается `python -m vliw run <сценарий>` (сразу на нужном
участке), `python -m vliw lab`, или клавишей `2` на экране выбора.

Вторая из двух глубоко описанных панелей — первая
[КОД](CODE_MODE.md). Здесь давление другое: не редактирование текста, а
чтение готового расписания и понимание, ПОЧЕМУ оно такое.

## Планировка экрана

Слева сверху вниз: **УЧАСТОК** (каталог сценариев), **РАСПИСАНИЕ** (решётка),
**GIT** (журнал команд и прогонов). Справа: **ЧИСЛА**, **ПОЧЕМУ ЗДЕСЬ**,
**ДИАГНОЗ**, **МАШИНА** — все четыре живут одной колонкой, `^B` сворачивает
и разворачивает её целиком.

## Фокус: решётка или строка ввода

Единственная тонкость, без которой половина клавиш кажется мёртвой:
**буквенные клавиши (`n`, `d`, `o`, `z`, `x`, `p`, `w`, `c`, `[`, `]`, `v`)
работают, только когда фокус на решётке, не в строке ввода.** `F2`
переключает фокус туда-сюда. Если нажали букву и ничего не произошло —
скорее всего, она просто напечаталась в строку команд; `F2`, и заново.

## Клавиши решётки (после `F2`)

| клавиша | действие |
|---|---|
| `↑↓←→` | курсор по клеткам — такт и канал |
| `Enter` | на обычной клетке — полный разбор такта в GIT (`/explain N`); на схлопнутом простое (`т.3–10 ⌄8`) — развернуть/свернуть именно этот участок простоя |
| `[` / `]` | курсор к предыдущей / следующей операции (не клетке — именно операции, пропуская пустые) |
| `n` / `N` | курсор к следующей / предыдущей находке ДИАГНОЗА |
| `c` | шаг по критическому пути от текущей клетки |
| `d` | развернуть ДИАГНОЗ целиком (все находки, не только топ) |
| `o` | развернуть GIT-журнал |
| `v` | переключить вид решётки: baseline ⇄ oracle (третий вид, «модель», — только чипом `вид` или командой `/view model`, клавиша между ним и остальными не ходит) |
| `z` | «Соседи»: подсветить только прямых предков и потомков курсорной операции, остальное притушить |
| `x` | «Все такты»: развернуть ВСЕ схлопнутые простои в отдельные строки разом |
| `p` | «Крит. путь»: погасить/включить жирную подсветку критического пути |
| `w` | «Широкие»: клетки 11 → 17 колонок (по делу только в развороте, где есть куда расти) |
| `2×клик` по любой панели | развернуть её на весь экран |
| `Esc` | свернуть развёрнутую панель; без неё — назад к выбору режима |
| `Tab` | справочник ИИ — работает и после разворота панели клавишей, и после 2×клика |

`z`, `x`, `p`, `w` — тумблеры отрисовки: каждый второй раз выключает то, что
включил первый.

## УЧАСТОК

Каталог из 17 встроенных сценариев — плитки, клик или `/run <имя>` выбирает
участок. Каждый — не случайный граф, а конкретный урок: `slotclash`
(монополия делителя), `divport` (то же самое другим числом), `wide_ilp`
(ресурсы важнее пути), и так далее. `/scenarios` в GIT покажет список
текстом.

## РАСПИСАНИЕ

Решётка: строки — такты, столбцы — каналы (`,0`…`,5`). Звёздочка у номера
канала — монопольный порт (например `,5*` для DIV/FDIV). Длинные простои
схлопываются в одну строку (`т.3–10 ⌄8`) — это обзорный режим, `x`
разворачивает всё, `Enter` на конкретной строке — только её.

## ЧИСЛА

Baseline, oracle, предел, выигрыш в тактах и процентах — сводка одного
взгляда. Разворот (`диагноз ▶`-соседняя кнопка «решётка ▶» в тулбаре, либо
2×клик) показывает, откуда взялся предел: цепочка критического пути и
ресурсное ограничение по отдельности, что из них шире.

## ПОЧЕМУ ЗДЕСЬ

Отвечает про клетку под курсором — включая ПУСТУЮ: почему в этом такте на
этом канале ничего не стоит (ждём операндов, порт занят, канал не принимает
эту операцию). Панель `z` («Соседи») на решётке — визуальный эквивалент
этого текста: то же самое глазами, без слов.

## ДИАГНОЗ

Список находок доктора: что теряет такты и почему, отсортировано по цене.
Тулбар: `◀ пред.` / `след. ▶` (то же, что `n`/`N` на решётке), `сброс
фильтра`, `решётка ▶` (курсор на клетку активной находки).

**Развёрнутый вид** (`d` или 2×клик) добавляет строку фильтра по
серьёзности: `все · критично · средне · мелко · предел`, с числом находок
каждого вида, и полный текст каждой — не только заголовок, а «почему» и
«что с этим делать».

## МАШИНА

Матрица возможностей портов: какая операция на каком канале живёт, у кого
латентность какая. Тулбар — сортировка по числу операций или по латентности.
Развёрнутый вид добавляет строку фильтра по имени операции (часть строки,
не точное совпадение).

## GIT

Журнал: не гит-репозиторий, а история прогонов этой сессии — что вводили,
что вышло. `/compare`, `/doctor`, `/asm`, `/load <файл.s>` печатают сюда же.
`Enter` на клетке решётки тоже сюда пишет разбор такта. Тулбар: `↻
повторить` последний прогон, `очистить` историю, `решётка ▶` развернуть
РАСПИСАНИЕ.

## Одна вещь, которую легко упустить

Решётка и правая колонка читают ОДИН И ТОТ ЖЕ курсор. Подсказки в ПОЧЕМУ
ЗДЕСЬ, находка в ДИАГНОЗЕ, шаг по цепочке — всё это движется курсором на
решётке, а не отдельными кнопками каждой панели. Первое, что стоит понять,
открыв РАЗБОР впервые: где курсор, там и ответ всех остальных панелей.
