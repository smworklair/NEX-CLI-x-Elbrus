# Режим ЯДРО — коротко

В отличие от [КОД](CODE_MODE.md) и [РАЗБОР](LAB_MODE.md), здесь не нужен
подробный разбор каждой кнопки — режим по сути один калькулятор с историей.
Открывается `python -m vliw --mode work` (позиционный `python -m vliw work`
на верхнем уровне сейчас уходит в другую команду — печатает справку
интерпретатора текстом, а не открывает экран; если нужен именно
полноэкранный ЯДРО, используйте `--mode work` или клавишу `4` на экране
выбора).

## Что это

Простой интерпретатор выражений (`a = 10`, `t = a*2`), который попутно
строит граф зависимостей из того, что вы считаете. Не про ассемблер и не
про готовые сценарии — про то, как граф вообще получается из обычных
вычислений, шаг за шагом.

## Три панели

- **ЛЕНТА** слева — история: что ввели, что вышло, сколько операций ушло
  в граф.
- **ИМЕНА** справа сверху — текущие переменные и их значения.
- **ПРОГРАММА** справа снизу — накопленный граф участка целиком.

## Основные слова

| команда | что делает |
|---|---|
| `a = 10`, `t = a*2` | обычное вычисление — задаёт имя, копит граф |
| `names` | показать имена и значения |
| `mem` | показать память |
| `list` | показать накопленный граф целиком |
| `go` | отдать граф в РАЗБОР и посчитать расписание |
| `reset` | обнулить накопленное и начать заново |

Готовые ядра для быстрого начала — `sum 8`, `dot 4`, `saxpy 8`, `fir 8`
(число — размер). Дальше `go` — и тот же граф открывается в РАЗБОРЕ со
всеми его панелями.
