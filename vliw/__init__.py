"""Прототип ИИ-планировщика инструкций для VLIW (калибровка по e2k-v6).

Пакеты:
  vliw.core     — логика (модель, граф, планировщики, метрики, проверка)
  vliw.ui       — визуализация (цвет, рамки, таблицы, логотип)
  vliw.learned  — каркас под будущую обученную модель (пока заглушка)
  vliw.cli      — единая точка входа: команда → core → ui → печать

Публичный API проекта — из vliw.core (переэкспортируется здесь для удобства).
"""

# Две записи одной и той же версии намеренно. `__version__` — машинная, по
# PEP 440, чтобы её понимали упаковщики и сравнивали как версию. `VERSION_LABEL`
# — человеческая, для баннера и подписей: «0.8.0a1» в шапке читается как
# опечатка, а «0.8 alpha» сразу говорит, что инструмент ещё не устоялся.
#
# 0.7 — три полноэкранных режима на Textual.
# 0.8 — редизайн командного слоя: команды сгруппированы по занятию (справка,
# палитра «/» и --help показывают разделы, «/help <команда>» рассказывает
# об одной), легаси-экран на curses удалён — полноэкранный только Textual,
# при проблемах откат в построчный режим.
# 0.9 — работа с НАСТОЯЩИМ кодом, а не только со сценариями:
#   · четвёртый полноэкранный раздел КОД — открыть свой `.s`, увидеть разбор,
#     расписание компилятора и замечания линтера прямо на строках;
#   · разбор вывода `lcc` доведён до настоящих файлов: спекулятивные операции
#     (`,sm`) больше не теряются, незнакомая мнемоника не выдаётся за
#     сложение, управляющие операции не занимают арифметические порты
#     (см. docs/REAL_VS_SYNTH.md — чем настоящий код отличается от обучающего);
#   · обученная модель запускается локально без GPU (`/learned`), с починкой
#     каналов и выбором из нескольких сэмплов (`--best-of`).
# Альфа: раскладки ещё будут меняться.
__version__ = "1.0.0b1"
VERSION_LABEL = "1.0 beta"

from .core import (
    DAG,
    DEFAULT_PROFILE,
    PROFILES,
    SCENARIOS,
    Candidate,
    DecisionStep,
    GreedyListScheduler,
    MachineModel,
    OracleScheduler,
    Schedule,
    Scheduler,
    SchedulingResult,
    compute_metrics,
    get_profile,
    get_scenario,
    list_schedule,
    random_dag,
)

__all__ = [
    "Candidate",
    "DecisionStep",
    "Scheduler",
    "SchedulingResult",
    "GreedyListScheduler",
    "list_schedule",
    "DAG",
    "SCENARIOS",
    "compute_metrics",
    "get_scenario",
    "random_dag",
    "DEFAULT_PROFILE",
    "PROFILES",
    "MachineModel",
    "get_profile",
    "OracleScheduler",
    "Schedule",
]
